$('.detail-cards-list').html("<%= j render 'cart', cart_items: @cart_items %>");
$('.pagination').html("<%= j render partial: 'shared/pagination', locals: { url: cart_index_path, collection: @cart_items } %>");
